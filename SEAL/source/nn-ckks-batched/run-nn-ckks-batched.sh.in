#!/bin/bash

RUN=1
OUTPUT_FILENAME=seal_batched_nn_cardio.csv

echo "t_keygen,t_input_encryption,t_computation,t_decryption" > $OUTPUT_FILENAME

while (( $RUN <= $NUM_RUNS ))
do
    RUN=$(( $RUN + 1))
    ./cardio_nn_batched
done
